# loantest

Playwright + TypeScript end-to-end coverage for the Bigbank loan application flow.

## Scripts

- `npm test` — run the Playwright suite
- `npm run report` — open the generated HTML report
