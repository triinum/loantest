{
  "name": "loantest",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + TypeScript end-to-end coverage for the Bigbank loan application flow.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^26.4.1",
    "typescript": "7.0.2"
  }
}
